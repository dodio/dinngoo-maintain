# 业务栈容器部署文档索引（php-server / dinngoo-site）

各**业务仓库**内已有「仅本服务容器」的部署说明（复制环境变量、Compose 命令、常见坑）：

| 仓库 | 文档 |
|------|------|
| **php-server**（ThinkPHP 后台栈） | **`php-server/deploy/README.md`** |
| **dinngoo-site**（Next C 端） | **`dinngoo-site/deploy/README.md`** |

跨栈架构（Caddy、分域、门禁）见 [**分域部署方案.md**](./分域部署方案.md)。

> **`supports/server-info/`** 目录（默认在 **[`.gitignore`](../.gitignore)** 中，多用于本机/内网整机说明）：与测试机、生产机路径、域名、SSH 别名等相关内容可放在该目录；团队若需版本管理可改为取消忽略或同步到内网 Wiki。
